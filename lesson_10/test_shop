import pytest
import allure

from selenium import webdriver
from ShopPage import ShopPage


URL = "https://www.saucedemo.com"
field_init = {
    "Username": "standard_user",
    "Password": "secret_sauce"
}
model = ['Sauce Labs Backpack',
         'Sauce Labs Bolt T-Shirt',
         'Sauce Labs Onesie'
         ]
FIO = {
    'first-name': 'РРІР°РЅ',
    'last-name': 'РРІР°РЅРѕРІ',
    'postal-code': '650400'
}
test_summa = '$58.29'
selector_init_user = "submit-button.btn_action"
selector_options = ["inventory_item",
                    "inventory_item_name",
                    "btn.btn_primary.btn_small.btn_inventory"
                    ]
selector_cart = ["shopping_cart_badge",
                 "shopping_cart_link",
                 "btn.btn_action.btn_medium.checkout_button"
                 ]
selector_user = 'submit-button.btn.btn_primary.cart_button.btn_action'
selector_result = 'summary_total_label'


@allure.epic("Р—Р°РєР°Р· РѕРґРµР¶РґС‹")
@allure.title("Р—Р°РєР°Р· РѕРґРµР¶РґС‹")
@allure.description("РўРµСЃС‚РёСЂРѕРІР°РЅРёРµ СЃР°Р№С‚Р° Р·Р°РєР°Р·Р° РѕРґРµР¶РґС‹")
@allure.severity("critical")
@pytest.mark.parametrize('result', [test_summa])
def test_shop(result):
    with allure.step("РћС‚РєСЂС‹РІР°РµРј СЃС‚СЂР°РЅРёС†Сѓ Р±СЂР°СѓР·РµСЂР°"):
        browser = webdriver.Chrome()
        shop_page = ShopPage(browser, URL)

    with allure.step("Р Р°Р±РѕС‚Р°РµРј СЃ РІС‹Р±РѕСЂРѕРј РѕРґРµР¶РґС‹"):
        shop_page.init_user(field_init, selector_init_user)
        shop_page.product_selection(selector_options[0],
                                    selector_options[1],
                                    selector_options[2],
                                    model)

    with allure.step("Р Р°Р±РѕС‚Р°РµРј СЃ РєРѕСЂР·РёРЅРѕР№ РїРѕР»СЊР·РѕРІР°С‚РµР»СЏ"):
        shop_page.go_cart(selector_cart[0], selector_cart[1], selector_cart[2])

    with allure.step("Р’РЅРѕСЃРёРј РґР°РЅРЅС‹Рµ РґР»СЏ РѕС„РѕСЂРјР»РµРЅРёСЏ Р·Р°РєР°Р·Р°"):
        shop_page.user_fileds(FIO, selector_user)

    with allure.step("РџСЂРѕРІРµСЂСЏРµРј РѕР±С‰СѓСЋ СЃСѓРјРјСѓ Р·Р°РєР°Р·Р°"):
        total = shop_page.total_summ(selector_result)
        assert total == result

    with allure.step("Р—Р°РєСЂС‹РІР°РµРј Р±СЂР°СѓР·РµСЂ"):
        browser.quit()